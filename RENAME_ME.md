# READ ME FIRST — Rename these before pushing to GitHub

To avoid hidden-file copy issues, three things in this zip were renamed:

| In this zip            | Rename to         |
| ---------------------- | ----------------- |
| `_HIDDEN_dockerignore` | `.dockerignore`   |
| `_HIDDEN_gitignore`    | `.gitignore`      |
| `_HIDDEN_github/`      | `.github/`        |

If uploading via the GitHub web UI, see the step-by-step instructions
Claude gave you — do the three renames on GitHub itself using the pencil
icon on each file, then delete this RENAME_ME.md file.
